add ctrl k behaviour to readme
add the part_dir thing to readme

no date in model folder name, model folder name is just value in name config (+ the current minute and second (-mmss) if name only already exists)