for fixed point target, let sl and tp be separetely definable, the same way atr target has default sl tp and label sl tp

want to add features that tell price change/direction since a given moment in time ago
i.e has price gone up or down since 5400 seconds
e.g PAST_DIR_5400_S would be a feature that tells how much price has changed 5400 seconds ago
and PAST_DIR_5400_T would be a feature that tells how much price has changed 5400 ticks ago
so the user can add multiple arbitrary 'PAST_DIR_*' configs
now i guess the value has to be normalized somewhat
so i guess a value between -1 and 1 or 0 and 1, where 0 to .5 is negative change and .5 to 1 is positive change.
i don't know which one is better for neural networks like this

let the root dir be neatly organized, let there be no .py or .mqh or .ex5 files in the root dir or even any other type of files in root dir, let them all be in their own folders
soso let the root have only folders, and not files, except for necessary ones like gitignore, readme and requirements

look closely at the codebase and pipeline and identify and list all flaws